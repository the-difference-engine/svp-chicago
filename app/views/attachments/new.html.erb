<h1>Add An Attachment:</h1>
<%= form_for @attachment, url: attachments_path, html: { multipart: true } do |form| %>
  <div>
    <%= form.file_field :attachment_file %>
  </div>
  <div>
    <%= form.submit 'Submit' %>  
  </div>    
<% end %>