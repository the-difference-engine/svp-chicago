<%= form_for(@mailer) do |f| %>
  <% if @mailer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailer.errors.count, "error") %> prohibited this mailer from being saved:</h2>

      <ul>
      <% @mailer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :login %><br>
    <%= f.text_field :login %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
